<div class="row">
	<div class="col-lg-12">
		<h1>Es hora de trabajar!</h1>
		<p>El contacto es la herramienta importante... <strong class="text-primary">entre ellos y nosostros!</strong> <a href="#" class="btn btn-default active pull-right text-normal">exportar grupos <span class="glyphicon glyphicon-download-alt"></span></a></p>
	</div>
</div>


<div class="row" style="margin-top: 20px;">
	<div class="col-lg-12">
		<div class="table-responsive">
			<table class="table">
				<thead>
				<tr>
					<th>Id</th>
					<th>Nombre</th>
					<th>Roles</th>
					<th></th>
				</tr>
				</thead>
				<tbody>
				{% for group in groups %}
					<tr>
						<td>{{ group.id }}</td>
						<td>{{ group.name }}</td>
						<td>{{ group.roles | join(', ') }}</td>
						<td>
							<div class="btn-group pull-right">
								<button type="button" class="btn btn-default active text-normal dropdown-toggle" data-toggle="dropdown" aria-expanded="false">revisar grupo</button>
								<ul class="dropdown-menu" role="menu">
									<li><a href="{{ path('fos_user_group_show', { 'groupName': group.name }) }}">Mostrar</a></li>
									<li><a href="{{ path('fos_user_group_edit', { 'groupName': group.name }) }}">Editar</a></li>
									<li><a href="#" data-toggle="modal" data-target="#MDL-eliminar{{ loop.index }}">Eliminar</a></li>
								</ul>
							</div>
							<div class="modal fade" id="MDL-eliminar{{ loop.index }}" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true" data-backdrop="static" data-keyboard="false">
								<div class="modal-dialog">
									<div class="modal-content">
										<div class="modal-body text-center">
											<h3 style="color:#ff7f00;">Estás seguro de querer eliminar este Grupo? {{ loop.index }}</h3>
										</div>
										<div class="modal-footer">
											<button type="button" class="btn btn-default" data-dismiss="modal">Cancelar</button>
											<a href="{{ path('fos_user_group_delete', {'groupName' : group.name}) }}" class="btn btn-default">Eliminar</a>
										</div>
									</div>
								</div>
							</div>
						</td>
					</tr>
				{% endfor %}
				</tbody>
			</table>
			<div class="clear-fix"><br/></div>
		</div>
	</div>
</div>

